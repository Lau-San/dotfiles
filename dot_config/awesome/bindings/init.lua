return {
    mod             = require 'bindings.mod',
    clicks          = require 'bindings.clicks',
    global_keys     = require 'bindings.global.keys',
    client_keys     = require 'bindings.client.keys',
    client_buttons  = require 'bindings.client.buttons'
}
