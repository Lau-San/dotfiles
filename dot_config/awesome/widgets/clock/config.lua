local theme         = require('theme')

local clock_config = {
    bg          = theme.widget_bg,
    fg          = theme.widget_fg,

    icon_color  = theme.colors.c4_500,
    icon_size   = theme.widget_icon_size,

    date_icon   = '',
    time_icon   = ''
}

return clock_config
